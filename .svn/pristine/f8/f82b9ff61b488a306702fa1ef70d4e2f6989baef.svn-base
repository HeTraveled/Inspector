package com.plan.service;

import java.util.List;

import com.plan.model.WeeksPlanMessage;
import com.util.page.PagedResult;

public interface WeeksPlanMessageSerivce {
	int creatweekplanmessage(WeeksPlanMessage weeksplanmessage);
	
	List<WeeksPlanMessage> selectthree(int wid);
	
	PagedResult<WeeksPlanMessage> selectall(Integer wid,Integer pageNo,Integer pageSize);
}
